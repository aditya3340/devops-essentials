name: "Demo github workflow"
on: push
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: log message
              run: echo "Hello"

            - name: added this workflow in feature branch
              run: echo "github-workflow-branch test"
    
        

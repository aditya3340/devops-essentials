# monitoring/apps/loki-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
  labels:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default

  source:
    repoURL: https://grafana.github.io/helm-charts
    targetRevision: 3.1.0

    chart: loki
    helm:
      valueFiles:
        - production-projects/app/cluster-addons/monitoring/helm-values/loki-values.yaml
  
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncPolicy:
    - CreateNamespace=true